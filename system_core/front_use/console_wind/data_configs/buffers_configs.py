"""
This module defines some constants values to use throughout the program
as configurations.

Constants:
    DEFAULT_GRID_CHAR (str): Stores value for settings as CHAR
    MAX_BACK_BUFFER_WIDTH (int): Stores value for settings as BUFFER_WIDTH
    MAX_BACK_BUFFER_HEIGHT (int): Stores value for settings as BUFFER_HEIGHT
    ADJUSTMENT_RANGE_TOP (int): Stores value for settings as RANGE_TOP
"""

# Front buffer default parameter(s)
DEFAULT_GRID_CHAR = "-"

# Back buffer default size parameters
MAX_BACK_BUFFER_WIDTH = 60
MAX_BACK_BUFFER_HEIGHT = 40

# Back buffer adjustment range 
ADJUSTMENT_RANGE_TOP = 1000
